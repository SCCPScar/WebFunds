/// A subset of `docs/02-Domain/05-Mysteries.md`'s "Creation Rules" —
/// the ones detectable without Banking, OCR, or Weaver: those need
/// features this app doesn't have yet (bank sync, receipts, per-
/// Transaction AI evidence).
enum MysteryReason { unknownMerchant, unknownCategory, manual }

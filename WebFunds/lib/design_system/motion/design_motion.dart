/// Motion tokens, re-exported here so every Design System token category
/// is importable from `lib/design_system/`. Actual implementation lives
/// in `lib/core/animation/`, per `Folder-Structure.md`.
library;

export '../../core/animation/motion_tokens.dart';